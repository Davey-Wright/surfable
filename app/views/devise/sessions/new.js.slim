|
  $.ajax().done(function(){
    open_modal("#{ j render "new" }");

    var error = "#{ j flash[:alert] }"

    if(error !== ''){
      $('form').prepend("<div class='form_errors'>error</div>");
    }
  });
