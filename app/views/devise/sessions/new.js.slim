|
  $.ajax().done(function(){
    open_modal("#{ j render "new" }");

    if("#{ j flash[:alert] }" !== ''){
      $('form').prepend('<div class="form_errors">#{ j flash[:alert] }</div>');
    }
  });
