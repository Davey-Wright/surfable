h1 Shwmae #{@user.first_name} #{@user.last_name}

p First Name #{@user.first_name}
p Last Name #{@user.last_name}
p Email #{@user.email}
p Spots #{@user.spots.count}
p Logged in with #{@user.provider unless @user.provider.nil?}

= link_to 'Settings', edit_user_registration_path,
  class: 'columns new_spot_btn',
  remote: true
