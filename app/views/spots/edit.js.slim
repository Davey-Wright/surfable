|
  $.ajax().done(function(){
    open_modal_with_content("#{ j render "edit" }");

    $('form').submit(function(){
      if($('form #spot_name').val() !== ''){
        $("#modal").foundation('close');
      }
    });
  });
